name: security_scoped_resource_example
description: Demonstrates how to use the security_scoped_resource plugin.

publish_to: 'none' #

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  security_scoped_resource:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
